{
  "name": "litty",
  "description": "Simple and accessible web component library made with Lit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:packages": "pnpm -r --filter ./packages/ --if-present build",
    "preinstall": "npx only-allow pnpm",
    "prepare": "pnpm build:packages",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "eslint": "^8.23.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3",
    "@zmrl/eslint-config": "^3.3.0",
    "vitest": "^0.23.2",
    "pnpm": "^7.11.0"
  }
}
